#include <stdio.h>

/**
 * program entry is main
 *
 * program prints single digit ints
 *
 * return: 0 if success
 */
int main(void)
{
char n;
for (n ='0'; n <= '9'; n++)
{
putchar (n);
}
putchar ('\n');

return (0);
}
