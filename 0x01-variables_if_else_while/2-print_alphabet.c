#include <stdio.h>

/**
 * main - entry to the function
 *
 * function: print lowercase alphabet single line
 */
int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
putchar(c);
putchar('\n');

return (0);
}
