#include <stdio.h>
/**
 * _abs-value abselute
 *
 * @n: the int to print
 *return: always0.
 */
int _abs(int n)
{
if (n < 0)
{
return (n * (-1));
}
else if (n == 0)
{
return (0);
}
else
{
return (0);
}
}
